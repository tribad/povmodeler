name: C/C++ CI

on:
  push:

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v2
    - name: Create build directory
      run: mkdir ./build
    - name: prepare build
      run: cd build; cmake ..
